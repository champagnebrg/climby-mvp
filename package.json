{
  "name": "climby-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --project climby-rules-test --only firestore \"node --test tests/firestore-competition-live.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
